# Tarea 2 - Proyecto 1

Resolución del sistema lorenz 63. Ejecutar el script de Python en terminal para asegurar su buen funcionamiento. Los paquetes necesarios para reproducir el código se pueden instalar usando el manejador de paquetes `conda` con el comando

```bash
conda env create -f environment.yml
```

